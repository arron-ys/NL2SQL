# Web Framework (Row 3 & 4)
fastapi>=0.100.0
uvicorn[standard]

# Data Validation (Row 5 - Strict v2)
pydantic>=2.0.0
pydantic-settings

# SQL Generation (Row 6)
pypika

# Database (Row 7 - Async Drivers)
sqlalchemy>=2.0.0
asyncpg         # PostgreSQL Driver
aiomysql        # MySQL Driver
cryptography>=41.0.0  # Required for MySQL 8 caching_sha2_password authentication

# LLM & AI (Row 8, 9, 10)
openai          # Official SDK
qdrant-client   # Vector DB
httpx           # Async HTTP for Jina AI

# Utilities (Row 11)
python-dotenv
pyyaml
loguru          # Structured Logging

# Testing (Test Framework)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0      # Code coverage (recommended)
freezegun>=1.2.0       # Time freezing (for test time isolation)

# Development Tools (Optional)
# black>=23.0.0        # Code formatting
# mypy>=1.0.0          # Type checking
# ruff>=0.1.0          # Fast linting